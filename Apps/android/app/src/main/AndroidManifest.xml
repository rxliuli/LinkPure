<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- followRedirect 规则要联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".LinkPureApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinkPure">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          ★ 唯一的入口。

          Android 上做不到 macOS 那种「零操作自动改写」：系统只让**有焦点**或**默认输入法**
          读剪贴板，而且连 clipboardChanged 事件都不投递给后台 app。
          PROCESS_TEXT 的内容随 intent 进来，完全不碰剪贴板，因此绕开了全部限制。

          theme 是半透明且不放任何内容 —— 静默模式下用户不该看到任何界面。
        -->
        <activity
            android:name=".ProcessTextActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/process_text_label"
            android:theme="@style/Theme.LinkPure.Silent">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".clean.CopyActionReceiver"
            android:exported="false" />

    </application>

</manifest>
